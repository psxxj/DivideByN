<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="travel">

<insert id="insertName" parameterType="hashMap" useGeneratedKeys="true" keyProperty="Travel_id">  
    <![CDATA[
    insert into travelDB
    (TravelName)
    values
    (#{TravelName})
    ]]>
</insert>

<select id="select" parameterType="hashMap" resultType="hashMap">
	<![CDATA[
		select
		TravelName
		from
		travelDB
		where
		Travel_id = #{Travel_id}
	]]>
</select>

</mapper>