<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="person">

<insert id="insert" parameterType="person" useGeneratedKeys="true" keyProperty="Person_id">  
    <![CDATA[
    insert into personDB
    (PersonName, Account, TravelName) 
    values
    (#{PersonName}, #{Account}, #{TravelName})
    ]]>
</insert>

<update id="update" parameterType="person">  
	<![CDATA[  
	update personDB set  
	PersonName = #{PersonName},  
	Account = #{Account}
	where  
	Person_id = #{PersonId}  
	]]>  
</update>  

<select id="getWhoInTravel" parameterType="travel" resultType="String">
	<![CDATA[
		select
		PersonName
		from
		personDB
		where
		travelName = #{travelName}
	]]>
</select>

<select id="getPersonBriefly" parameterType="travel" resultType="person">
	<![CDATA[
		select
		PersonName,
		Role,
		Difference
		from
		personDB
		where
		travelName = #{travelName}
	]]>
</select>

<select id="getPersonDetail" parameterType="person" resultType="person">
	<![CDATA[
		select
		PersonName,
		Account,
		Role,
		Difference
		from
		personDB
		where
		Person_id = #{Person_id}
	]]>
</select>



</mapper>	